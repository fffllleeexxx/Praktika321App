<Window x:Class="Praktika321App.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Praktika321App"
        mc:Ignorable="d"
        Style="{DynamicResource BackgroundColor}"
        Title="LoginWindow" Height="450" Width="800">
    <Grid>
        <Button Style="{DynamicResource buttonStyle}" Content="QR-code" Click="QRButton_Click" Width="100" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20"/>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Login" Margin="0,0,0,10"/>
            <TextBox x:Name="txtLogin" Width="200" Margin="0,0,0,10"/>
            <TextBlock Text="Password" Margin="0,0,0,10"/>
            <PasswordBox Style="{DynamicResource PasswordStyle}" x:Name="txtPassword" Width="200" Margin="0,0,0,10"/>
            <Button Style="{DynamicResource buttonStyle}" Content="Login" Click="LoginButton_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
